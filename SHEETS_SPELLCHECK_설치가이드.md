# 구글시트 맞춤법 검사 — 설치 가이드

요청문구(제목/내용) 셀에 한국어 맞춤법 검사를 붙이는 Google Apps Script입니다.
파일: [`sheets_spellcheck.gs`](./sheets_spellcheck.gs)

## 어떤 기능인가

- 시트 상단에 **`🔤 맞춤법 검사`** 메뉴가 생깁니다.
- 검사할 셀(제목/내용 열)을 선택 → **[선택 영역 검사]** 클릭.
- 오탈자가 있는 셀은 **연한 빨강**으로 강조되고, **셀 메모**에 `원문 → 수정후보 (설명)`이 붙습니다.
- 셀 원본 텍스트는 **바꾸지 않습니다** (검수용 표시만).
- **[표시 지우기]** 로 강조/메모를 한 번에 제거.

다음(Daum) 한국어 맞춤법 검사기 엔진(`dic.daum.net`, HTTPS)을 사용합니다.
(과거 부산대 검사기는 서버가 폐기되어 사용 불가.)

## 설치 (5분)

1. 검사할 구글시트를 엽니다.
2. 상단 메뉴 **확장 프로그램 → Apps Script** 클릭.
3. 열린 편집기의 기존 `Code.gs` 내용을 **모두 지우고**, 이 저장소의 [`sheets_spellcheck.gs`](./sheets_spellcheck.gs) 내용을 **전부 붙여넣기**.
4. 💾 저장(Ctrl/Cmd+S).
5. 편집기 상단 함수 목록에서 **`onOpen`** 을 선택하고 **▶ 실행** 한 번 → 권한 승인 팝업이 뜨면 **본인 구글 계정으로 허용**.
   - "이 앱은 Google에서 확인하지 않았습니다" 화면 → **고급 → (안전하지 않음)으로 이동 → 허용**. 본인이 만든 스크립트라 안전합니다.
6. 시트로 돌아와 새로고침하면 상단에 **`🔤 맞춤법 검사`** 메뉴가 보입니다.

## 사용법

1. 제목 또는 내용 열에서 검사할 범위를 드래그로 선택.
2. **`🔤 맞춤법 검사 → 선택 영역 검사`** 클릭.
3. 진행 후 오류 셀이 빨강으로 강조됨 → 셀에 마우스를 올리면 수정 후보가 표시됨.
4. 검토·수정 완료 후 **`표시 지우기`** 로 정리.

> 셀이 많으면 시간이 걸립니다(서버 보호를 위해 셀마다 약간 대기). **한 번에 200셀 이하**로 나눠 검사하는 것을 권장합니다.

## 옵션 조정 (`sheets_spellcheck.gs` 상단 상수)

| 상수 | 기본값 | 설명 |
|------|--------|------|
| `SPELLER_URL` | `https://dic.daum.net/grammar_checker.do` | 다음 맞춤법 검사기 엔드포인트(HTTPS) |
| `SLEEP_MS` | `350` | 셀 사이 대기(ms). 차단되면 값을 키우기 |
| `MAX_CELLS` | `500` | 한 번에 검사 허용 최대 셀 수 |
| `ERR_BG` | `#FCE8E6` | 오류 셀 강조 색 |

## 사용자 사전 (브랜드 오타 잡기)

맞춤법 검사기는 표준 국어 사전 기반이라 **브랜드명·고유명사 오타**(예: `카즈`→`카츠`)는 못 잡습니다. 이럴 땐 사용자 사전을 쓰세요.

1. **`🔤 맞춤법 검사 → 사용자 사전 만들기/열기`** 클릭 → `맞춤법사전` 탭이 생깁니다.
2. 표에 규칙을 채웁니다.

   | 틀린표현 | 올바른표현 | 메모 |
   |----------|-----------|------|
   | 카즈 | 카츠 | 브랜드명 |
   | 썸머 | 서머 | 외래어 표기 |

3. 이제 `선택 영역 검사`를 하면 **다음 검사기 결과 + 사용자 사전**이 함께 적용되어, 셀에 해당 표현이 있으면 `틀린표현 → 올바른표현 (메모) (사용자 사전)`으로 표시됩니다.

- 코드 수정 없이 표만 채우면 됩니다. 규칙은 원하는 만큼 추가 가능.
- `틀린표현`과 `올바른표현`이 같으면(예시 행) 무시됩니다.

## 오탐 방지

검사 전에 아래를 자동으로 제거해서 맞춤법기가 오류로 잡지 않게 합니다.

- 치환 변수: `#{고객명}`, `{변수}`
- URL, 이메일
- 이모지·기호

## 자주 나오는 문제

| 증상 | 원인 | 해결 |
|------|------|------|
| `DNS 오류` | 옛 부산대 서버 폐기됨 | 최신 스크립트(다음 엔진)로 교체됨 — `sheets_spellcheck.gs` 다시 붙여넣기 |
| `검사기 응답 형식 오류` | 다음 페이지 구조 변경/차단 | 잠시 후 재시도, 지속되면 알려주세요 |
| `HTTP 5xx` / 너무 느림 | 검사 서버 과부하 | `SLEEP_MS` 키우고 범위를 줄여 재시도 |
| 메뉴가 안 보임 | onOpen 권한 미승인 | 편집기에서 `onOpen` 수동 실행 → 권한 허용 후 새로고침 |
| 멀쩡한 단어가 오류로 | 검사기 특성(신조어·브랜드명) | 표시만 참고, 실제 수정은 담당자 판단 |

## 한계

- 부산대 검사기는 **비공식 API**라 서버 상태에 따라 불안정할 수 있습니다.
- "완벽 자동 교정"이 아니라 **의심 구간 표시 → 사람이 확인** 도구로 사용하세요.
